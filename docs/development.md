# Development

Requirements:

- Rust 1.98+
- Node.js 22+
- pnpm 11+
- macOS is required for the native Tauri shell; the UI can run in any supported browser.

Commands will be documented here as the implementation becomes runnable.

